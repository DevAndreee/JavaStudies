public class Car {
    // Attribute: defines the car's model
    String model;

    // Attribute: defines the car's year
    int year;

    // Attribute: defines the car's color
    String color;
}

/*
CLASSES IN JAVA:
 A class is a blueprint used to create objects.
 It defines attributes (characteristics) and, later, methods (behaviors).
 Syntax: 'public class ClassName { ... }'
 Each attribute has a type and a name.
 A class does not execute anything by itself — it just defines structure.
*/